create or replace PROCEDURE greetings(X INT ,S OUT INT) 
AS 

BEGIN

FOR XY IN (SELECT FIRST_NAME,LAST_NAME, SALARY FROM EMPLOYEES  WHERE EMPLOYEE_ID = X )
LOOP

DBMS_OUTPUT.PUT_LINE(XY.FIRST_NAME||'  ' ||XY.LAST_NAME||' '||XY.SALARY); 

END LOOP;   
END;